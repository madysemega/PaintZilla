import { PolygonStrokeRenderer } from './polygon-stroke-renderer';

describe('PolygonStrokeRenderer', () => {
  it('should create an instance', () => {
    expect(new PolygonStrokeRenderer()).toBeTruthy();
  });
});
